package yixia.lib.core.widget;

import android.os.Bundle;
import android.support.annotation.Nullable;

public interface OnPositiveButtonClickListener {
    void onPositiveButtonClicked(@Nullable Bundle data);
}